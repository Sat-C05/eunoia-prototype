generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  
  email     String   @unique
  password  String   // Hashed
  name      String

  // Relationships
  assessments Assessment[]
  bookings    Booking[]
  moodLogs    MoodLog[]
}

model Assessment {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())

  userId         String? // Keep optional for now to support legacy data if any
  user           User?   @relation(fields: [userId], references: [id])
  
  assessmentType String   @default("PHQ9")
  totalScore     Int
  severity       String
  rawAnswers     String
}

model Booking {
  id          String       @id @default(cuid())
  createdAt   DateTime     @default(now())

  studentName  String
  studentEmail String?
  reason       String?

  slot        DateTime
  status      String @default("PENDING")

  userId      String?
  user        User?    @relation(fields: [userId], references: [id])
}

model MoodLog {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  userId    String?
  user      User?   @relation(fields: [userId], references: [id])
  
  mood      Int
  note      String?
}
